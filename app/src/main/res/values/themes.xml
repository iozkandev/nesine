<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NesineApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NesineApp" parent="Base.Theme.NesineApp" />

    <!-- Splash Screen API theme. Applied to MainActivity; hands off to the main
         theme via postSplashScreenTheme once the activity is ready. -->
    <style name="Theme.NesineApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="postSplashScreenTheme">@style/Theme.NesineApp</item>
    </style>
</resources>
